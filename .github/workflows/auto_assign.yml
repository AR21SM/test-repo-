name: Auto-assign issues from PR

on:
  pull_request:
    types: [opened]

permissions:
  issues: write
  pull-requests: read

jobs:
  assign-issue:
    runs-on: ubuntu-latest
    steps:
      - name: Extract issue number from PR body and commits, then assign
        uses: actions/github-script@v7
        with:
          script: |
            const owner = context.repo.owner;
            const repo = context.repo.repo;
            const prNumber = context.payload.pull_request.number;
            const assignee = context.payload.pull_request.user.login;

            const issueNumbers = new Set();
            const pattern = /(?:closes|fixes|resolves)\s+#(\d+)/gi;

            // 1. Check the Pull Request body for linked issues
            const prBody = context.payload.pull_request.body;
            if (prBody) {
              for (const match of prBody.matchAll(pattern)) {
                issueNumbers.add(match[1]);
              }
            }

            // 2. Check all Commit messages for linked issues
            const { data: commits } = await github.rest.pulls.listCommits({
              owner: owner,
              repo: repo,
              pull_number: prNumber,
            });

            for (const commit of commits) {
              const commitMessage = commit.commit.message;
              if (commitMessage) {
                for (const match of commitMessage.matchAll(pattern)) {
                  issueNumbers.add(match[1]);
                }
              }
            }
            
            // 3. Exit early if no issues were found
            if (issueNumbers.size === 0) {
              console.log('No linked issues found in PR body or commit messages.');
              return;
            }

            // 4. Assign all unique issues that were found
            for (const issueNumber of issueNumbers) {
              console.log(`Assigning issue #${issueNumber} to @${assignee}`);
              try {
                await github.rest.issues.addAssignees({
                  owner: owner,
                  repo: repo,
                  issue_number: issueNumber,
                  assignees: [assignee],
                });
                console.log(`Successfully assigned issue #${issueNumber} to @${assignee}`);
              } catch (error) {
                console.log(`Failed to assign issue #${issueNumber}: ${error.message}`);
              }
            }